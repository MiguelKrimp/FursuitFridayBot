{
  "name": "fursuitfriday",
  "version": "0.0.1",
  "description": "Fursuit Friday Bot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "yarn tsc",
    "serve": "yarn tsc && node ./build/index.js"
  },
  "author": "Ardukai",
  "license": "MIT",
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "eris": "^0.15.1",
    "fs-extra": "^10.0.0"
  }
}
